//
// Created by harry cohen on 2025-01-15.
//
#include "Event.h"

string Event::getDescription() const{
    if (!= pack){
    return nameOfMonster + "(power" + to_string(monster.level) + ", loot" + to_string(monster.loot) +", damage" + to_string(monster.damage) + ")";}
    else{
        return "Pack of"+ int+ "members ( power" + to_string(monster.level) + ", loot" + to_string(monster.loot) +", damage" + to_string(monster.damage) + ")";}
    }
}